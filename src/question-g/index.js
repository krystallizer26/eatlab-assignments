import React, { useState } from "react";

const QuestionG = () => {
   // const [authResult, setAuthResult] = useState(0);

   return (
      <div>
         <p> Under construction ...</p>
      </div>
   );
};

export default QuestionG;
